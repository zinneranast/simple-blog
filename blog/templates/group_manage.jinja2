{% extends "header.jinja2" %}

{% block view_content %}
    <div style="margin: 10px; padding: 10px">
        <h4>Managing the "{{context.__name__}}"</h4>
        <br />
        <a href="{{request.resource_url(context, 'add_member')}}">Add member</a>
        <br /><br />
        <a href="{{request.resource_url(context, 'delete_group')}}">Delete group</a>
        <br /><br />
        <a href="{{request.resource_url(context)}}">Posts</a>
    </div>
{% endblock %}